<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.code.creator.mapper.PostgresDatabaseMapper">

    <!-- 查询 PostgreSQL 表注释 -->
    <select id="findTableComment" resultType="string">
        SELECT obj_description(table_name::regclass)
        FROM information_schema.tables
        WHERE table_catalog = current_database()
          AND table_name = #{table}
    </select>

    <!-- 查询 PostgreSQL 表列信息 -->
    <select id="findTableColumns" resultType="java.util.HashMap">
        SELECT *,
               col_description(table_name::regclass, ordinal_position) column_comment
        FROM information_schema.columns
        WHERE table_catalog = current_database()
          AND table_name = #{table}
        ORDER BY ordinal_position
    </select>

</mapper>
